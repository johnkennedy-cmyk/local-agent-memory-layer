# LAML Server Environment Configuration
# Copy this file to .env in the laml-server directory and fill in your actual values

# =============================================================================
# VECTOR BACKEND (choose one)
# =============================================================================
# firebolt = use Firebolt for long-term memory and vector search (default)
# elastic  = use Elasticsearch for long-term memory and vector search (sessions/working memory still use Firebolt)
LAML_VECTOR_BACKEND=firebolt

# =============================================================================
# OPTION 1: Local-Only Setup (Recommended)
# Uses Firebolt Core (local) + Ollama (local) - no cloud dependencies
# =============================================================================

# Firebolt Core (Local Database) - used for sessions, working memory, and long-term memory when LAML_VECTOR_BACKEND=firebolt
FIREBOLT_USE_CORE=true
FIREBOLT_CORE_URL=http://localhost:3473
FIREBOLT_DATABASE=laml

# Ollama (Local LLM for embeddings and classification)
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3:8b
OLLAMA_EMBEDDING_MODEL=nomic-embed-text
OLLAMA_EMBEDDING_DIMENSIONS=768

# =============================================================================
# ELASTICSEARCH (when LAML_VECTOR_BACKEND=elastic)
# =============================================================================
# ELASTICSEARCH_URL=http://localhost:9200
# ELASTICSEARCH_INDEX=laml_long_term_memories
# Auth: set either ELASTICSEARCH_API_KEY or ELASTICSEARCH_USERNAME + ELASTICSEARCH_PASSWORD (leave unused ones empty)
# ELASTICSEARCH_API_KEY=
# ELASTICSEARCH_USERNAME=
# ELASTICSEARCH_PASSWORD=
# ELASTICSEARCH_SSL_VERIFY=true
# ELASTICSEARCH_EMBEDDING_DIMENSIONS=768

# =============================================================================
# OPTION 2: Cloud Setup (Alternative)
# Uses Firebolt Cloud + OpenAI - requires accounts and API keys
# =============================================================================

# OpenAI API (for embeddings and augmentation)
# OPENAI_API_KEY=your-openai-api-key-here

# Firebolt Cloud Connection
# FIREBOLT_USE_CORE=false
# FIREBOLT_ACCOUNT_NAME=your-account-name
# FIREBOLT_CLIENT_ID=your-client-id
# FIREBOLT_CLIENT_SECRET=your-client-secret
# FIREBOLT_DATABASE=your-database-name
# FIREBOLT_ENGINE=your-engine-name
